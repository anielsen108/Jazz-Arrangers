import { defineConfig } from 'astro/config';

// Deployed as a GitHub Pages project site:
// https://anielsen108.github.io/Jazz-Arrangers/
export default defineConfig({
  site: 'https://anielsen108.github.io',
  base: '/Jazz-Arrangers',
});
