{
  "name": "jazz-arrangers",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "portraits:source": "node scripts/source-wikimedia-portraits.mjs --write",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
